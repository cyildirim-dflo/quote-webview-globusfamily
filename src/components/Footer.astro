---
import trustpilot from "../assets/icons/trustpilot.png";
interface Props {
    companyName: string;
}

const { companyName } = Astro.props as Props;
---

<div class="flex flex-col w-full bg-white">
    <div>
        <p
            class="tracking-[1px] font-sans font-medium text-white text-lg text-center px-2 bg-accentalt py-[60px]"
        >
            © {new Date().getFullYear()} {companyName}. All rights reserved.
        </p>
    </div>
</div>
